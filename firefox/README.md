# Firefox vertical tabs
## Important
Configuration based on [ranmaru22](https://codeberg.org/ranmaru22)'s, so all credits go to them.

## Installation
1. Go to `about:config` and set `toolkit.legacyUserProfileCustomizations.stylesheets` to `true`.
2. Go to `about:profiles` and open your profile's `Root Directory`.
3. Create a `chrome` folder if there isn't one and put `userChrome.css` in it.
4. Install [Sidebery](https://addons.mozilla.org/en-US/firefox/addon/sidebery/) allowing it to run in private windows.
5. In Sidebery's settings, go to `Help > Import addon data` and choose `sb_settings.json`.
6. Go to `Styles editor` and paste the contents of `sb_style.css`.
7. Restart Firefox.
